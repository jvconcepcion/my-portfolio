'use client'

import React from 'react'

type Props = {}

// const mapComponentToJSX = (component) => {
//   const { type, props } = component;
//   switch (type) {
//     case 'h3':
//       return <h3 {...props} />;
//     default:
//       return null;
//   }
// };

const SchemaToComponent = (props: Props) => {
  return (
    <div>SchemaToComponent</div>
  )
}

export default SchemaToComponent